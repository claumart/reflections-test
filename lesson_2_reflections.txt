1. What happens when you initialize a repository? Why do you need to do it?
R// When you initialize a repository Git creates a .git directory that is a hidden folder, you need to do that cause in that directory is where git save the commits you make

2. How is the staging area different from the working directory and the repository? What value do you think it offers?
R// In the staging area there are only the files we'll commit, the value that this offers is choose which the commit will affect

3. How can you use the staging area to make sure you have one commit per logical change?
R// BY only including the realated files with the logical change

4. What are some situations when branches would be helpful in keeping your history organized? How would branches help?
R//When you wanna make alternative versions of your program, branches would help to have a better management of your versions

5. How do the diagrams help you visualize the branch structure?
R// Allow you visualize the history in a graphic way

6. What is the result of merging two branches together? Why do we represent it in the diagram the way we do?
R// The result is one single branch which have the name of the checkouted branch